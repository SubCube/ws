{
  "name": "server",
  "version": "1.0.0",
  "main": "dist/server.js",
    "scripts": {
    "start": "tsc && node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.1.6",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.5"
  }
}
